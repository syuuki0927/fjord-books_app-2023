<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to Book.human_attribute_name("edit_button"), edit_book_path(@book) %> |
  <%= link_to Book.human_attribute_name("back_to_list_button"), books_path %>
  <%= button_to t(Book.human_attribute_name("destroy_button")), @book, method: :delete %>
</nav>
