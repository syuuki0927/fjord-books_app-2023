<h1><%= t('views.common.title_edit', name: Comment.model_name.human.downcase) %> </h1>

<%= render "form", comment: @comment %>

<br>

<div>
  <%= link_to t('.back', name: @comment.commentable.model_name.human.downcase), @comment.commentable %>
</div>
